language: r

matrix:
  include:
    - os: linux
      r: release
      r: devel
    - os: osx
      r: release
      disable_homebrew: true

sudo: false
cache: packages
warnings_are_errors: true



notifications:
  email:
    on_success: change
    on_failure: change

